# Submissions

Each stage has specific required columns:

- Stage1: `id,label` (label is 0/1)
- Stage2: `id,label_0,label_1,...` (binary columns per class)
- Stage4: `id,alignment_score,pass` (score float, pass boolean)
